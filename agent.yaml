agent:
  id: "internettoday"
  team: "dev"
  runtime: aks
  interaction_model: "standalone"
  guardrail_profile: "strict"

identity:
  sp_client_id: "f35de06e-b996-424a-98a2-cb207070f154"
  keyvault_url: "https://kv-shop-poc.vault.azure.net/"

tools: []

evaluation:
  suite: default-strict
  ci_gate: true
  pass_threshold: 0.95
  datasets:
    - id: golden-dataset
      version: v1.0
      source: repo
      path: evals/datasets/golden-dataset.json

output_policy:
  violation_threshold: 3
  window_minutes: 1
  action: alert
  notify_webhook: 

prompts:
  active_version: v1.0
  source: prompts/system.txt
